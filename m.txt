refactor: implement all 14 architectural quick wins

Performance:
- Wrap SlideCanvas in React.memo with custom comparator
- Stabilize CodeBlock highlightCode effect deps (useMemo syntaxKey)
- Memoize currentFont lookup in useCarousel

Cleanup:
- Remove unused lucide-react dependency
- Remove dead Annotation type/interface and .annotation-arrow CSS
- Remove unused font exports (CODE_FONTS, HEADING_FONTS, BODY_FONTS)
- Un-export 9 internal-only functions across 5 files

Error handling:
- Convert importJson to async/await with proper try/catch
- Add ErrorBoundary component around preview and filmstrip

Architecture:
- Import SlideCanvas directly in useExport (remove prop indirection)
- Extract PORTRAIT_FORMAT/SQUARE_FORMAT constants to types
- Consolidate Sidebar from 15 individual props to action-group pattern
- Extract SLIDE layout constants to constants/slideLayout.ts